[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "forestroutemvp"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
# keep runtime dependencies in requirements.txt to avoid duplication here

[tool.setuptools.packages.find]
where = ["."]
include = ["frp*"]
